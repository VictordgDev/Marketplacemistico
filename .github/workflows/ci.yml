name: CI - Testes e Validação

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Instalar dependências
      run: npm install

    - name: Executar análise estática (Linter)
      run: npm run lint

    - name: Verificar vulnerabilidades em dependências (SCA)
      run: npm run audit || true # Não quebra a build agora, mas reporta

    - name: Executar testes unitários e de integração
      run: npm test

    # No futuro podemos adicionar Playwright aqui para testes E2E
    # - name: Install Playwright Browsers
    #   run: npx playwright install --with-deps
    # - name: Run Playwright tests
    #   run: npx playwright test
